---
platforms:
  ubuntu1404:
    sauce: true
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci"
    test_targets:
    - "//..."
  # Disabled, because it doesn't add much over testing on ubuntu1404 only and we
  # otherwise run out of Sauce Labs tunnels.
  # ubuntu1604:
  #   sauce: true
  #   build_targets:
  #   - "//..."
  #   test_flags:
  #   - "--test_tag_filters=-noci"
  #   test_targets:
  #   - "//..."
  # ubuntu1804:
  #   sauce: true
  #   build_targets:
  #   - "//..."
  #   test_flags:
  #   - "--test_tag_filters=-noci"
  #   test_targets:
  #   - "//..."
  macos:
    sauce: true
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci"
    test_targets:
    - "//..."
  # Disabled, because Windows isn't really supported yet.
  # windows:
  #   sauce: true
  #   build_targets:
  #   - "//..."
  #   test_flags:
  #   - "--test_tag_filters=-noci"
  #   test_targets:
  #   - "//..."
